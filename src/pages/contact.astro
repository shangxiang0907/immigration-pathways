---
import SiteLayout from "../layouts/SiteLayout.astro";
import { site } from "../../site.config.mjs";
---

<SiteLayout title={`Contact — ${site.name}`} description={`Contact ${site.name}.`}>
  <article class="prose"><p class="eyebrow">Contact</p><h1>Contact us</h1><p>Add an approved public contact method before publication. Do not expose a private mailbox or personal information by accident.</p></article>
</SiteLayout>

# Production controls canonical URLs and sitemap generation, not indexing permission.
SITE_BUILD_MODE=preview
# Keep false until release gates 1-5 pass and the owner explicitly authorizes indexing.
SITE_INDEXING_ENABLED=false
